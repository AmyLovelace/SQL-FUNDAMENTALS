-- SELECTING ROWS UNDER A CONDITION OF EQUALITY
-- To select a specific value, we will use the equality operator (=).

-- Example:

-- SELECT * FROM products WHERE price = 100;
-- Exercise: Select the username of the user from the users table with id equal to 2.

--CORRECT ANSWER:

SELECT username from users WHERE id = 2;
